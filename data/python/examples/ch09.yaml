# GENERATED by workbooks/python-for-ml/examples/run.py from examples/ch09/.
# Do not edit: change the example and re-run `make examples`.

data:
  - code: |-
      import torch
      from torch.utils.data import DataLoader, TensorDataset
      X = torch.arange(10.0).reshape(5, 2)
      y = torch.tensor([0, 1, 0, 1, 1])
      dataset = TensorDataset(X, y)
      # A Dataset hands out one example by index.
      len(dataset), dataset[0]
    output: "(5, (tensor([0., 1.]), tensor(0)))"
  - code: |-
      # A DataLoader groups examples into batches, in order unless you pass shuffle=True.
      for xb, yb in DataLoader(dataset, batch_size=2):
          print(xb.shape, yb.tolist())
    output: |-
      torch.Size([2, 2]) [0, 1]
      torch.Size([2, 2]) [0, 1]
      torch.Size([1, 2]) [1]
devices:
  - code: |-
      import torch
      from torch import nn
      device = 'cuda' if torch.cuda.is_available() else 'cpu'
      model = nn.Linear(2, 1)
      # A module's .to() moves its weights in place and returns the module itself.
      model.to(device) is model
    output: "True"
  - code: |-
      # Every batch has to follow it; a mismatch raises. This page runs on the CPU,
      # so a float64 batch stands in for one left on the wrong device.
      model(torch.ones(3, 2, dtype=torch.float64))
    output: "RuntimeError: mat1 and mat2 must have the same dtype, but got Double and Float"
  - code: "model(torch.ones(3, 2, dtype=torch.float64).to(device, torch.float32)).shape"
    output: "torch.Size([3, 1])"
eval-mode:
  - code: |-
      import torch
      from torch import nn
      model = nn.Sequential(nn.Linear(4, 4), nn.Dropout(p=0.5))
      x = torch.ones(1, 4)
      # A fresh model is in training mode: dropout zeroes each value with probability 0.5, afresh each call.
      model.training
    output: "True"
  - code: "torch.equal(model(x), model(x))"
    output: "False"
  - code: |-
      # Evaluation mode turns dropout off, so the same input gives the same output.
      model.eval()
    output: |-
      Sequential(
        (0): Linear(in_features=4, out_features=4, bias=True)
        (1): Dropout(p=0.5, inplace=False)
      )
  - code: "torch.equal(model(x), model(x))"
    output: "True"
  - code: |-
      # eval() doesn't stop autograd recording; torch.no_grad() does.
      model(x).requires_grad
    output: "True"
  - code: |-
      with torch.no_grad():
          print(model(x).requires_grad)
    output: "False"
loss:
  - code: |-
      import torch
      from torch import nn
      # Raw scores (logits) for 3 examples and 2 classes, and each one's class.
      logits = torch.tensor([[2.0, -1.0], [0.5, 0.5], [-1.0, 3.0]])
      target = torch.tensor([0, 1, 1])
      nn.CrossEntropyLoss()(logits, target)
    output: "tensor(0.2533)"
  - code: |-
      # Classes must be integers; float labels raise.
      nn.CrossEntropyLoss()(logits, target.float())
    output: "RuntimeError: expected target dtype to be Long or Byte, but got Float"
  - code: |-
      # Softmaxing first doesn't raise; it just gives a different, wrong loss.
      nn.CrossEntropyLoss()(logits.softmax(dim=1), target)
    output: "tensor(0.4520)"
  - code: |-
      # For numbers rather than classes: the mean squared error.
      nn.MSELoss()(torch.tensor([2.5, 0.0]), torch.tensor([3.0, 0.0]))
    output: "tensor(0.1250)"
model:
  - code: |-
      import torch
      from torch import nn
      # A model is a class: layers made in __init__, used in forward.
      class Classifier(nn.Module):
          def __init__(self):
              super().__init__()
              self.hidden = nn.Linear(2, 4)
              self.out = nn.Linear(4, 2)

          def forward(self, x):
              return self.out(torch.relu(self.hidden(x)))
      model = Classifier()
      # Call the model, not forward: model(x) runs forward for you.
      model(torch.randn(5, 2)).shape
    output: "torch.Size([5, 2])"
  - code: |-
      # The same model as a chain of layers.
      nn.Sequential(nn.Linear(2, 4), nn.ReLU(), nn.Linear(4, 2))
    output: |-
      Sequential(
        (0): Linear(in_features=2, out_features=4, bias=True)
        (1): ReLU()
        (2): Linear(in_features=4, out_features=2, bias=True)
      )
  - code: |-
      # Every weight and bias the optimizer will step: 2*4 + 4 + 4*2 + 2.
      sum(p.numel() for p in model.parameters())
    output: "22"
  - code: |-
      # An embedding looks rows up by id: 10 ids, 3 numbers each.
      nn.Embedding(10, 3)(torch.tensor([7, 2, 7])).shape
    output: "torch.Size([3, 3])"
save-load:
  - code: |-
      import tempfile
      from pathlib import Path
      import torch
      from torch import nn
      model = nn.Sequential(nn.Linear(2, 4), nn.ReLU(), nn.Linear(4, 2))
      # The state_dict: every weight by name. Numbers 0 and 2 are the two Linear layers.
      {name: tuple(t.shape) for name, t in model.state_dict().items()}
    output: "{'0.weight': (4, 2), '0.bias': (4,), '2.weight': (2, 4), '2.bias': (2,)}"
  - code: |-
      # path is a model.pt file in a scratch folder.
      torch.save(model.state_dict(), path)
      # Load into a fresh model of the same shape.
      fresh = nn.Sequential(nn.Linear(2, 4), nn.ReLU(), nn.Linear(4, 2))
      fresh.load_state_dict(torch.load(path))
    output: "<All keys matched successfully>"
  - code: |-
      x = torch.randn(3, 2)
      torch.equal(fresh(x), model(x))
    output: "True"

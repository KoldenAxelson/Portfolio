# GENERATED by workbooks/python-for-ml/examples/run.py from examples/ch01/.
# Do not edit: change the example and re-run `make examples`.

containers:
  - code: |-
      # a list keeps order and repeats
      runs = ['a', 'b', 'a']
      runs.append('d')
      runs
    output: "['a', 'b', 'a', 'd']"
  - code: |-
      # a dict maps keys to values
      lr = {'a': 0.1, 'b': 0.05}
      lr['c'] = 0.01
      lr
    output: "{'a': 0.1, 'b': 0.05, 'c': 0.01}"
  - code: |-
      # a set keeps each value once, in no guaranteed order
      {3, 1, 3, 2}
    output: "{1, 2, 3}"
  - code: "[x * x for x in range(6) if x % 2 == 0]"
    output: "[0, 4, 16]"
dataclass:
  - code: |-
      from dataclasses import dataclass
      @dataclass
      class Run:
          name: str
          lr: float
      Run('a', 0.1)
    output: "Run(name='a', lr=0.1)"
  - code: "Run('a', 0.1) == Run('a', 0.1)"
    output: "True"
  - code: |-
      # the hint says float, but nothing checks it
      bad = Run('b', '0.05')
      bad.lr * 2
    output: "'0.050.05'"
functions:
  - code: |-
      loss = 0.4213
      f'loss {loss:.2f} after {3 * 2} epochs'
    output: "'loss 0.42 after 6 epochs'"
  - code: |-
      def log(*values, **fields):
          return values, fields
      log(1, 2, lr=0.1)
    output: "((1, 2), {'lr': 0.1})"
  - code: |-
      def train(lr, epochs):
          return f'lr={lr}, epochs={epochs}'
      config = {'lr': 0.1, 'epochs': 5}
      train(**config)
    output: "'lr=0.1, epochs=5'"
lazy:
  - code: |-
      from pathlib import Path
      path = Path('data/runs.csv')
      with path.open() as f:
          header = f.readline()
      header, f.closed
    output: "('run,lr,loss\\n', True)"
  - code: |-
      def losses(path):
          with path.open() as f:
              next(f)  # skip the header
              for line in f:
                  yield float(line.split(',')[2])
      g = losses(path)
      next(g)
    output: "0.42"
  - code: "list(g)"
    output: "[0.31, 0.58]"
  - code: "list(g)"
    output: "[]"

# GENERATED by workbooks/python-for-ml/examples/run.py from examples/ch02/.
# Do not edit: change the example and re-run `make examples`.

dropping-axes:
  - code: |-
      import numpy as np
      a = np.arange(12).reshape(3, 4)
      a[1].shape
    output: "(4,)"
  - code: "a[1:2].shape"
    output: "(1, 4)"
  - code: "a[:, 1].shape"
    output: "(3,)"
  - code: "a[:, 1:2].shape"
    output: "(3, 1)"
  - code: "a[a > 5].shape"
    output: "(6,)"
  - code: "a[a[:, 0] > 2].shape"
    output: "(2, 4)"
flat:
  - code: |-
      import numpy as np
      a = np.arange(6).reshape(2, 3)
      a.ravel()
    output: "array([0, 1, 2, 3, 4, 5])"
  - code: "np.shares_memory(a, a.ravel()), np.shares_memory(a, a.flatten())"
    output: "(True, False)"
  - code: "a.T"
    output: |-
      array([[0, 3],
             [1, 4],
             [2, 5]])
making:
  - code: |-
      import numpy as np
      a = np.array([[1, 2, 3], [4, 5, 6]])
      a.shape, a.dtype
    output: "((2, 3), dtype('int64'))"
  - code: "np.zeros((2, 3))"
    output: |-
      array([[0., 0., 0.],
             [0., 0., 0.]])
  - code: "np.full((2, 3), 7)"
    output: |-
      array([[7, 7, 7],
             [7, 7, 7]])
  - code: "np.arange(0, 12, 3)"
    output: "array([0, 3, 6, 9])"
  - code: "np.linspace(0, 1, 5)"
    output: "array([0.  , 0.25, 0.5 , 0.75, 1.  ])"
  - code: |-
      rng = np.random.default_rng(0)
      rng.integers(0, 10, size=4)
    output: "array([8, 6, 5, 2])"

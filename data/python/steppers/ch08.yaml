# GENERATED by workbooks/python-for-ml/examples/run.py from examples/ch08/.
# Do not edit: change the example and re-run `make examples`.

autograd:
  caption: "Autograd Example"
  code:
    - "import torch"
    - "x = torch.tensor([1., 2., 3.])"
    - "w = torch.tensor([0.5, -1., 2.], requires_grad=True)"
    - "b = torch.tensor(1., requires_grad=True)"
    - "y = w @ x + b"
    - "w.grad"
    - "y.backward()"
    - "(w @ x + b).backward()"
    - "w.detach().numpy()"
  steps:
    - line: 1
      label: "torch.tensor"
      panels:
        - title: "x"
          meta: "shape (3,) · torch.float32"
          kind: "grid"
          cells:
            - ["1.", "2.", "3."]
      note: "Made from a list of floats: shape (3,), dtype torch.float32."
    - line: 2
      label: "requires_grad"
      panels:
        - title: "w"
          meta: "shape (3,) · torch.float32 · requires_grad"
          kind: "grid"
          cells:
            - ["0.5", "-1.", "2."]
      note: "requires_grad=True: autograd records everything computed from w."
    - line: 3
      label: "requires_grad"
      panels:
        - title: "b"
          meta: "shape () · torch.float32 · requires_grad"
          kind: "text"
          text: "tensor(1., requires_grad=True)"
      note: "A single number is a tensor too, with shape ()."
    - line: 4
      label: "autograd"
      panels:
        - title: "y"
          meta: "shape () · torch.float32 · requires_grad"
          kind: "text"
          text: "tensor(5.5000, grad_fn=<AddBackward0>)"
      note: "0.5·1 − 1·2 + 2·3 + 1 = 5.5. grad_fn names the operation that made y, the +."
    - line: 5
      label: ".grad"
      panels:
        - title: "w.grad"
          kind: "text"
          text: "None"
      note: "No gradient yet: .grad is None until backward runs."
    - line: 6
      label: "backward"
      panels:
        - title: "x"
          meta: "shape (3,) · torch.float32"
          kind: "grid"
          cells:
            - ["1.", "2.", "3."]
        - title: "w.grad"
          meta: "shape (3,) · torch.float32"
          kind: "grid"
          cells:
            - ["1.", "2.", "3."]
        - title: "b.grad"
          meta: "shape () · torch.float32"
          kind: "text"
          text: "tensor(1.)"
      note: "The gradient of y with respect to w is x, and with respect to b it is 1."
    - line: 7
      label: ".grad"
      panels:
        - title: "w.grad"
          meta: "shape (3,) · torch.float32"
          kind: "grid"
          cells:
            - ["2.", "4.", "6."]
        - title: "b.grad"
          meta: "shape () · torch.float32"
          kind: "text"
          text: "tensor(2.)"
      note: "A second backward adds to .grad: 2, 4, 6 now, not 1, 2, 3."
    - line: 8
      label: "detach"
      panels:
        - title: "w.detach().numpy()"
          meta: "shape (3,) · float32"
          kind: "grid"
          cells:
            - ["0.5", "-1.", "2."]
      note: "detach gives the same values with no graph, and then .numpy() works."
